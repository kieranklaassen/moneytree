<%= form_with(model: marketplace_order, local: true) do |form| %>
  <% if marketplace_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(marketplace_order.errors.count, "error") %> prohibited this marketplace_order from being saved:</h2>

      <ul>
        <% marketplace_order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
